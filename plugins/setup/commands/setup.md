---
description: "Set up a project for Claude Code. Scans the repo, generates a CLAUDE.md with stack info, commands, key paths, and conventions. Recommends plugins to install."
---

Invoke the setup:setup skill and follow its instructions EXACTLY.

Your job is to analyze this project and generate a CLAUDE.md. Do NOT start building features, fixing code, or doing anything else. Follow the skill's process: scan, generate, present for review, then save if approved.
